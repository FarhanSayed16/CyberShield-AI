# Archived: `doc/backend/`

This tree is a **stale snapshot / working copy** of backend experiments and should **not** be treated as the live application.

### Use instead

| Live path | Purpose |
| :--- | :--- |
| [`../../backend/`](../../backend/) | Production FastAPI app |
| [`../../cybersentinel-ml-api/`](../../cybersentinel-ml-api/) | Optional remote ML service |

Duplicate Dockerfiles, env samples, and test dumps under this folder may contradict the current codebase (Gemini-only defaults, auth, health fields, etc.).
